# Benchmark fixture

Objective: make `python -m unittest -v` pass by correcting the smallest complete defect. Preserve the test and public `add(left, right)` signature. The route named in the benchmark prompt is explicitly selected for measurement.
