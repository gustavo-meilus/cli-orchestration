# Adoption Plan

1. Install in one temporary project and verify the selected adapter only.
2. Use direct mode for clear low-risk work; explicitly invoke orchestration for a representative material batch.
3. Compare Implementer → Verifier with Scout → Implementer → Verifier only where discovery uncertainty is plausible.
4. Record defects caught, elapsed time, observable tokens/cost, turns, tool calls, repeated reads, tests, rework, churn, and user intervention.
5. Promote an adapter or optional stage only when source and field evidence supports it.

Keep OpenSpec canonical throughout. Use migration dry-run before upgrading a six-role installation or state v1 ledger.
