## 1. Baseline and Reconciliation

- [x] 1.1 Create a file-level ownership and reconciliation map for the current repository, the archived `simplify-cross-cli-orchestration` work, and the inspected v2.0.0 artifact identity/checksum; classify each relevant asset as retain, adapt, migrate, or retire and each document as shipped-current, contributor-policy, or target-planning input without changing canonical OpenSpec-owned paths.
- [x] 1.2 Add or revise contract tests that encode ordered route precedence, route-selection boundaries, fresh-Verifier identity, pure orchestration, one-writer ownership, route-specific acceptance, fail-closed worker availability, optional high-assurance roles, framework authority, CLI-neutral behavior, public entry compatibility, legacy-state handling, and aligned public versions before changing their implementations.
- [x] 1.3 Revalidate current authoritative Codex CLI, Claude Code, Copilot CLI, and OpenSpec documentation for every discovery, invocation, configuration, and support claim used by the adapters; record source dates and unresolved evidence gaps.

## 2. Adaptive Portable Core

- [x] 2.1 Replace the six-role default protocol and managed project policy with cumulative predicates for explicit orchestration, high risk, uncertainty, material implementation, minimum-context read-only orchestration, useful read-only parallelism, and direct execution; route substantive selection uncertainty to Scout rather than primary-thread exploration and never manufacture an implementation cycle for read-only work.
- [x] 2.2 Implement CLI-neutral Scout, Implementer, and fresh-Verifier contracts, define freshness as a new non-implementer context independent of model identity, and map retained Planner, Validator, Final Auditor, and specialist contracts into clearly optional high-assurance resources.
- [x] 2.3 Revise control packets for context-affinity batches, compact worker results, dependency-aware dispatch, disjoint read-only parallelism, and one active writer per overlapping ownership boundary.
- [x] 2.4 Implement bounded rework so the first concrete defect returns to the same Implementer when safely available, one recorded replacement handles the correction otherwise, a different fresh Verifier checks it, and a second failure produces cause diagnosis instead of an automatic loop.
- [x] 2.5 Make durable orchestration state optional for cross-session or multi-batch routes, update its schema and tooling to the minimum resumable fields, migrate only unambiguous valid legacy claims with preservation and fresh reconciliation, block unsafe conversions actionably, and verify stateless direct and single-batch work remains valid.

## 3. Framework and OpenSpec Composition

- [x] 3.1 Replace the legacy orchestrated OpenSpec bridge behavior with a thin route-aware integration that invokes canonical apply and verification workflows without recreating proposal, specs, design, tasks, or acceptance planning, and treats canonical verification as evidence rather than an automatic orchestration PASS.
- [x] 3.2 Add portable integration guidance for TDD, repository task systems, and external development frameworks that enforces explicit user/repository/framework authority precedence, blocks unresolved same-level conflicts, and distinguishes acceptance-criteria ownership from the selected route's acceptance decision.
- [x] 3.3 Add preservation and composition tests proving installation, migration, direct apply, and orchestrated apply do not modify, copy, package, or shadow canonical `.agents/skills/openspec-*` resources.
- [x] 3.4 Verify normal OpenSpec routes do not automatically stack equivalent Reviewer, Validator, Final Auditor, and canonical verification gates, while distinct high-risk gates remain expressible.

## 4. Codex, Claude, and Copilot Adapters

- [x] 4.1 Refactor adapter resources around the shared logical roles and routes so CLI-specific files contain only native discovery, invocation, configuration, fail-closed worker-availability behavior, and optional hardening behavior.
- [x] 4.2 Update and test the Codex CLI adapter, including the no-selection compatibility default, explicit orchestration entry, native agent mapping, unavailable-worker BLOCKED behavior, current configuration schema, and compatibility with canonical OpenSpec skills.
- [x] 4.3 Update and test the Claude Code adapter, including native or general-purpose agent mapping, explicit orchestration entry, unavailable-worker BLOCKED behavior, current discovery rules, and documented limitations.
- [x] 4.4 Update and test the Copilot CLI adapter, including native or general-purpose agent mapping, explicit orchestration entry, unavailable-worker BLOCKED behavior, current discovery rules, and documented limitations.
- [x] 4.5 Keep CLI-native hardening profiles optional and verify absence of an equivalent hardening primitive does not alter portable-core behavior or falsely fail base compatibility.

## 5. Distribution and Migration

- [x] 5.1 Update the installer, wrappers, manifest, and verifier for explicit single- or multi-tool selection, Codex-only behavior when selection is omitted, deduplicated core resources, selected-adapter-only deployment, preserved logical skill names and native adapter invocations, and one aligned `2.0.0` package/protocol version surface.
- [x] 5.2 Implement dry-run and apply migration from legacy six-role-default installations and resumable state, with managed-file ownership checks, recoverable backups, conservative state conversion, removal or relocation of obsolete package-owned assets, and preservation of unrelated files.
- [x] 5.3 Harden repeat installation and managed-file replacement against copied read-only or restrictive source attributes, and add a regression fixture based on the expanded v2.0.0 source behavior.
- [x] 5.4 Expand clean-install, repeat-install, upgrade, rollback, temporary-project, and mixed-CLI fixtures to verify deterministic output and actionable failure without partial manifest publication.

## 6. Documentation and Release Surface

- [x] 6.1 Rewrite the README, usage, workflow, architecture, governance, and managed project policy documentation around the two execution modes, ordered KISS routing, pure orchestration invariant, context-affinity batching, fresh-Verifier definition, bounded rework, and the distinction between repository contributor policy and installed runtime policy.
- [x] 6.2 Update OpenSpec integration, installation, migration, troubleshooting, source-verification, and model-policy documentation with ownership boundaries, support evidence, and rollback procedures.
- [x] 6.3 Publish an evidence-based Codex/Claude/Copilot support matrix generated or checked against manifest evidence, using exactly `experimental|supported`, distinguishing static package checks from fresh-process native smoke results, and retaining failed or unverified integrations as experimental with reasons.
- [x] 6.4 Reconcile one breaking-release version across `VERSION`, protocol header, README, changelog, package manifest, checksums, and verifier output after the final managed-file set is known, and enforce repository cache hygiene.

## 7. Validation, Field Evidence, and Tuning

- [x] 7.1 Run the complete repository test and manifest suites plus strict OpenSpec validation, fixing all regressions within the change scope.
- [x] 7.2 Run fresh project/user install, Codex-default install, explicit/mixed-CLI install, repeat-install, legacy installation/state migration, rollback, public-entry compatibility, restrictive-attribute, version-consistency, and canonical OpenSpec byte-preservation checks from clean fixtures.
- [x] 7.3 Run fresh-process discovery, required generic-worker creation, explicit-invocation, and unavailable-worker smoke tests for each available CLI; capture CLI version and reproducible evidence in the manifest, reconcile narrative claims, and set each adapter's `experimental` or `supported` status from the result.
- [x] 7.4 Benchmark representative workloads across direct, Implementer → Verifier, and Scout → Implementer → Verifier routes, recording correctness, verifier-caught defects, elapsed time, observable tokens and cost, turns, tool calls, repeated reads, test runs, rework, diff churn, and user interventions.
- [x] 7.5 Tune route guidance, optional stages, and adapter model recommendations only where benchmark evidence demonstrates material value; document inconclusive measurements without promoting them to defaults.
- [x] 7.6 Perform canonical OpenSpec verification of the completed implementation against all four capability specs and leave field-validation limitations explicit before archive or release.
