# Claude Code repository bridge

Read and follow `AGENTS.md` for shared repository policy. Invoke `/orchestrator-work-protocol` only when its adaptive routing triggers or the user explicitly requests it. Canonical OpenSpec skills remain OpenSpec-owned.
